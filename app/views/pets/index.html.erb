<h1>Lost Pets :c</h1>
<div class="container">
  <div class="row">
    <div class="col-6">
      <div>
      <% @pets.each do |pet| %>
        <h2><%= link_to pet.name, pet_path(pet) %></h2>
      <% end %>
      </div>
    </div>
  </div>

  <%= link_to "Add Pet", new_pet_path %>
</div>
